'use client'

import styled from "styled-components"

export const Container = styled.div`
    scroll-snap-type:y mandatory;
    overflow-y: scroll;
    height: 100vh;
    overflow-x: hidden;
    margin-right: -16px;
`